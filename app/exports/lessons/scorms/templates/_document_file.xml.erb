<file href="html/documents/#{document.id}/#{document.url}"> <!-- TODO schorm sistema url documento con replace -->
  <metadata>
    <lom>
      <general>
        <identifier>
          <catalog>URI</catalog>
          <entry><%= document.url %></entry> <!-- TODO schorm sistema url documento con replace -->
        </identifier>
      </general>
      <technical>
        <format>document/<%= document.extension %></format>
        <size><%= document.size %></size>
        <location><%= document.url %></location> <!-- TODO schorm sistema url documento con replace -->
      </technical>
    </lom>
  </metadata>
</file>
