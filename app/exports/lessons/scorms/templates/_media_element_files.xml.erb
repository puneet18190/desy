<% if media_element.sti_type == 'Video' %>
  <file href="html/media_elements/videos/#{media_element.id}/#{media_element.mp4_url}"> <!-- TODO schorm sistema urls con replace -->
    <%= render_with_default_context :partial => 'media_element_file_metadata.xml', :locals => {:media_element => media_element, :extension => 'mp4'} %>
  </file>
  <file href="html/media_elements/videos/#{media_element.id}/#{media_element.webm_url}"> <!-- TODO schorm sistema urls con replace -->
    <%= render_with_default_context :partial => 'media_element_file_metadata.xml', :locals => {:media_element => media_element, :extension => 'webm'} %>
  </file>
<% elsif media_element.sti_type == 'Image' %>
  <file href="html/media_elements/videos/#{media_element.id}/#{media_element.url}"> <!-- TODO schorm sistema urls con replace -->
    <%= render_with_default_context :partial => 'media_element_file_metadata.xml', :locals => {:media_element => media_element, :extension => 'jpeg'} %> <!-- TODO schorm estrai anche estensione, invece di mettere quella finta -->
  </file>
<% elsif media_element.sti_type == 'Audio' %>
  <file href="html/media_elements/videos/#{media_element.id}/#{media_element.m4a_url}"> <!-- TODO schorm sistema urls con replace -->
    <%= render_with_default_context :partial => 'media_element_file_metadata.xml', :locals => {:media_element => media_element, :extension => 'm4a'} %>
  </file>
  <file href="html/media_elements/videos/#{media_element.id}/#{media_element.ogg_url}"> <!-- TODO schorm sistema urls con replace -->
    <%= render_with_default_context :partial => 'media_element_file_metadata.xml', :locals => {:media_element => media_element, :extension => 'ogg'} %>
  </file>
<% end %>
