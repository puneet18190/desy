<%
  url_type ||= UrlTypes::EXPORT
  image      = slide.media_elements_at
  author     = slide.lesson.user
  subject    = slide.lesson.subject
%>

<% if image -%>
  <%= render partial: 'OEBPS/slides/image_media_elements_slide_style.xhtml', locals: { image_media_elements_slide: image } %>
<% end -%>

<div class="image"></div>
<div class="subject"><%= subject.description %></div>
<h1 class="title"><%= slide.lesson.title %></h1>
<div class="author"><%= author.full_name %></div>