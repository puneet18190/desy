<% if @ok %>
  <% @playlist.each do |l| %>
    $('#virtual_classroom_lesson .jspPane').append("<%= j(render(:partial => "virtual_classroom/view_single_playlist", :locals => {:virtual => l})) %>");
  <% end %>
  $('#virtual_classroom_playlist').data('offset', <%= @offset + @limit %>);
<% end %>
