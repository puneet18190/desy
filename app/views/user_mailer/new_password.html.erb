<%= t('mailer.reset_password.message', :password => @password, :name => @user.full_name, :desy => SETTINGS['application_name']).html_safe %>
<br>
<p>
  <%= link_to root_url(*host_and_port(login: true)), root_url(*host_and_port(login: true)) %>
</p>

