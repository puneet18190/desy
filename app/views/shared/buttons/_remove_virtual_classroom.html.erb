<%= render :partial => 'shared/buttons/standard_ajax_button', :locals => {:name => Buttons::REMOVE_VIRTUAL_CLASSROOM, :item => item, :destination => destination, :reload_url => add_js_parameters(request.url, item.id)} %>
