<% if @errors %>
  $('.form_error').removeClass('form_error');
  $('#slides ._error_messages').html('<%= j standard_form_error_messages(@errors) %>');
  <% @error_fields.each do |err| %>
    $('#slides #<%= err %>').addClass('form_error');
    if($('#slides #<%= err %>_placeholder').val() == '') {
      $('#slides #<%= err %>').val('');
    }
  <% end %>
<% else %>
  $('.form_error').removeClass('form_error');
  $('#slides ._error_messages').html('');
  window.location = '/lessons/<%= @lesson.id %>/slides/edit/';
<% end %>
