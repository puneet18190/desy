<%= form_tag save_slide_path(slide.lesson_id, slide.id), :method => :get, :remote => true, :id => "form-#{slide.id}" do %>
  <h2 class="only-title">
    <%= text_area_tag :title, (slide.title.blank? ? t('captions.enter_title') : slide.title), :class => '_lesson_editor_placeholder', :data => {:placeholder => slide.title.blank?} %>
  </h2>
<% end %>
