<%= form_tag save_slide_path(slide.lesson_id, slide.id), :method => :get, :remote => true, :id => "form-#{slide.id}" do %>
  <% video = slide.video_at(1) %>
  <% if video %>
  <span class="spacer20"></span>
    <%= render :partial => 'lesson_editor/slides/filled_parts/video', :locals => {:slide => slide, :position => 1, :video => video} %>
  <% else %>
    <%= hidden_field_tag :media_element_1, "", :class=> "media-tag-cover-1" %>
    <span class="spacer20"></span>
    <div class="cover-choose-image _cover_choose_image_1">
      <a class="show-gallery video2-p1 _show_video_gallery_in_lesson_editor"><%= image_tag "choose-video.png" %></a>
      <br/>
      <%= t('captions.choose_video') %>
    </div>
  <% end %>
<% end %>
