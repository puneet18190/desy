<!DOCTYPE html>
<html class="lesson-viewer-layout <%= controller_html_class %> <%= action_html_class %>">
  <%= render :partial => "shared/head" %>
  <body>
    <%= render :partial => "shared/popups" %>
    <%= render :partial => "shared/loading" %>
    <a class="navLeft" title="<%= t('titles.slides.back') %>"></a>
    <div class="container">
      <%= yield %>
    </div>
    <a class="navRight" title="<%= t('titles.slides.forward') %>"></a>
  </body>
</html>
