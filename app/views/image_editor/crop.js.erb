if(<%=@crop%>){
  $("#cropped_image").imgAreaSelect({ 
    hide: true
  });
  $("._toggle_crop").removeClass("current");
  $("#image_wrapper img").attr("src","<%=@editing_url%>/<%= @custom_filename %>");
  $('input[name="cropped_image"]').val("<%= @custom_filename %>");
  resetSelect();
}