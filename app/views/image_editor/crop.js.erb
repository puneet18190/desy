/*if(<%= !@crop_url.blank? %>) {
  $('#cropped_image').imgAreaSelect({
    hide: true
  });
  $('._toggle_crop').removeClass('current');
  $('#image_wrapper img').attr('src', '<%= @crop_url %>');
  resetImageEditorCrop();
} else {
  // TODO metti 'x' rossa
}*/

// aggiornare dimensioni reali
// ripristinare azione del form
// svuotare tutto il resto
console.log('fatto -- <%= @new_url %>');
