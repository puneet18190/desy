<div class="empty _empty_media_elements _load_media_element">
  <div class="createButton">
    <a class="_empty_media_elements_hover"></a>
  </div>
  <div class="fltlft"><% if emptied %>
    <%= t('dashboard.emptied_media_elements') %>
  <% else %>
    <%= t('dashboard.empty_media_elements') %>
  <% end %>
  <h1><%= t('dashboard.load_new_media_element_now') %></h1></div>
</div>
