alert("<%= @errors %>");
<% if @errors.length > 0 %>
  showErrorPopUp("<%= @errors %>");
  console.log("<%= @errors %>");
<% elsif @success %>
  showOkPopUp("<%= t('other_popup_messages.correct.user_update') %>");
<% end %>
