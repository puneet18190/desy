<div id="media_element_popup_<%= media_element.id %>" style="display:none">
  <% if media_element.info_changeable %>
    <div class="_change_info_container" style="display:none">
      <%= form_tag change_media_element_info_path(media_element.id) do %>
        <%= text_field_tag 'title', 'title' %>
        <%= text_area_tag 'description', 'description' %>
        <%= text_area_tag 'tags', 'tags' %>
        <%= submit_tag 'cambia' %>
      <% end %>
    </div>
  <% end %>
  <div class="_media_element_pop_up_container">
    <%= render :partial => "media_elements/previews/#{media_element.sti_type.downcase}" %>
    <div class="fltlft" style="width:280px;">
      <h1><%= media_element.sti_type %></h1>
      <h2><%= media_element.title %></h2>
      <p><%= media_element.description %></p>
    </div>
  </div>
  <div style="float:left; width:100%; background-color:#fff;">
    <div class="menuIconButton" style="float:left;  padding-left:10px">
      <%= render :partial => "shared/buttons/#{Buttons::EDIT}" %>
      <% if media_element.info_changeable %>
        <%= render :partial => "shared/buttons/#{Buttons::CHANGE_INFO}" %>
      <% end %>
    </div>
    <div class="menuIconButton" style="float:right">
      <% if media_element.is_reportable %>
        <%= render :partial => "reports/media_element", :locals => {:media_element => media_element} %>
      <% end %>
      <a class="close _close_popup" data-param="<%= media_element.id %>"></a>
    </div>
  </div>
</div>
