	<div class="contentInterno">
		<h1><%= lesson.subject.description %></h1>
		<h2><%= lesson.title %></h2>
		<p><%= lesson.description %></p>
	</div>
	<%= render :partial => "shared/action_buttons", :locals => {:item => lesson, :destination => ButtonDestinations::EXPANDED_LESSON} %>
	
