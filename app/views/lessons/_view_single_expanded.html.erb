<div id="lesson_expanded_<%= lesson.id %>" class="boxViewSingleLesson">
  <div class="contentInterno">
    <h1><%= lesson.subject.description %></h1>
    <h2><%= lesson.title %></h2>
    <p><%= lesson.description %></p>
  </div>
  <%= render :partial => "lessons/action_buttons", :locals => {:lesson => lesson, :destination => ButtonDestinations::EXPANDED_LESSON} %>
</div>
