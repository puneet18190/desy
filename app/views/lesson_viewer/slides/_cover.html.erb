<%
  image = slide.image_at(1)
  author = slide.lesson.user
  subject = slide.lesson.subject
  image_height = resize_height(image.media_element.width,image.media_element.height,"cover") if !image.nil?
  image_width = resize_width(image.media_element.width,image.media_element.height,"cover") if !image.nil?
%>

<div class="bigViewLesson slideCover">
  <div class="cover">
    <% if image %>
      <% if is_horizontal?(image.media_element.width,image.media_element.height,"cover")%>
        <%= image_tag image.media_element.url, :style => "left:#{image.alignment}px; height: 540px; width: #{image_width}" %>
      <% else %>
        <%= image_tag image.media_element.url, :style => "top:#{image.alignment}px; width: 900px;height: #{image_height}" %>
      <% end %>
    <% end %>
  </div>
  <div class="bg_text">
    <div class="lessonTitle"><%= slide.lesson.title %></div>
  </div>
  <div class="lessonSubject">
    <%= subject.description %><span><%= t('captions.author') %>: <strong><%= author.full_name %></strong></span>
  </div>
</div>
