<span class="tooltipHelp" id="tooltip_help">
  <%= image_tag("freccia_tooltip.png", :class => "callout") %>
  <p>
    <strong><%= t("help.#{@where}.first.title", :desy => SETTINGS['application_name']).html_safe %></strong><br>
    <%= t("help.#{@where}.first.content", :desy => SETTINGS['application_name']).html_safe %>
  </p>
  <p>
    <strong><%= t("help.#{@where}.second.title", :desy => SETTINGS['application_name']).html_safe %></strong><br>
    <%= t("help.#{@where}.second.content", :desy => SETTINGS['application_name']).html_safe %>
  </p>
  <p>
    <strong><%= t("help.#{@where}.third.title", :desy => SETTINGS['application_name']).html_safe %></strong><br>
    <%= t("help.#{@where}.third.content", :desy => SETTINGS['application_name']).html_safe %>
  </p>
</span>
