<li id="notification_<%= notification.id %>" class="_single_notification" data-param="<%= notification.id %>">
  <div class="notification_gatto expanded <%= notification.seen ? 'current'.html_safe : '' %>"><div style="overflow:hidden; width:210px; height:45px;"><%= strip_tags remove_title_from_notification(notification.message_with_time).html_safe %></div></div>
  <div class="cancelTooltip _destroy_notification" data-param="<%= notification.id %>" title="<%= t('titles.notifications.remove') %>"></div>
  <div class="_expanded_notification" style="display:none">
    <%= notification.message_with_time.html_safe %>
  </div>
</li>
