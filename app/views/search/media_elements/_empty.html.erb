<% if word.blank? %>
  <div class="emptyFilter"><h1><%= t('search.media_elements.empty_filter') %></h1></div>
<% else %>
  <div class="emptyFilter"><h1><%= t('search.media_elements.empty_word', :word => word) %></h1></div>
<% end %>
