<div id="media_elements_search_amount">
  <% if tags.empty? %>
    <p><%= t('search.media_elements.result', :num => amount.to_s) %></p>
  <% else %>
    <p><%= t('search.media_elements.result_with_tags', :num => amount.to_s) %></p>
  <% end %>
</div>
