$('.tooltipForm:visible').parent().find('._reportable_lesson_icon').click();
<% if @resp %>
  showOkPopUp('<%= j t("other_popup_messages.correct.report") %>');
<% else %>
  showErrorPopUp('<%= j @error %>');
<% end %>
